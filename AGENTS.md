# 安全作业管理项目协作规则

## 项目约束

- 项目名称：安全作业管理。
- 当前阶段：方案原型。
- 计划完成日期：2026-08-28。
- 产品负责人和业务规则最终决定人为仓库所有者；出现业务冲突时不得自行改变规则，应记录待确认问题。
- 正式 PRD 交付格式为 HTML。
- PC 与移动端不要求页面和功能完全一致。
- 移动端当前范围仅包括：作业申请、作业监控查看。超出范围的移动端功能必须先确认。

## 事实源优先级

发生冲突时按以下顺序处理：

1. 产品负责人已确认并写入 PRD 的业务规则。
2. `Ai产物文件-prd文档/安全作业管理_PRD.html`。
3. `docs/decisions/` 中已接受的决策记录。
4. PC、移动端现有原型。
5. `参考的产品原型/` 和制度附件；这些仅作为参考，不直接覆盖已确认产品规则。

## 修改边界

- 每项任务开始前说明目标、事实源、允许修改范围和验收标准。
- 一项任务只处理一个主题，禁止顺手重构无关文件。
- 不覆盖或删除用户已有的未提交修改。
- `参考的产品原型/`、制度附件和原始表格默认只读。
- PRD、PC、移动端产生差异时，需要在 PR 中说明差异及原因。
- 修改业务规则时，必须同步更新 PRD 和 `docs/CHANGELOG.md`。
- 修改原型但不改变业务规则时，也要在 PR 中说明影响端和验证结果。

## Git 协作

- 禁止直接在 `main` 上开发；使用 `codex/<类型>-<主题>` 分支。
- 每个分支对应一个 Issue 或一个边界清晰的任务。
- 提交前只暂存本任务文件，禁止使用 `git add .`、`git add -A` 或 `git add --all`。
- 合并必须经过 Pull Request 和另一位成员审核。
- 提交信息使用中文，格式建议为：`类型: 简要说明`。
- 类型可用：`产品`、`PRD`、`PC`、`移动端`、`文档`、`修复`、`构建`。

## 版本规则

- 当前产品基线：`v2.0.1--product-baseline`。
- 版本采用 `v主版本.模块版本.修订版本`。
- 新增模块或模块级重大更新：增加第二个数字，并将第三个数字归零，例如 `v2.1.0`。
- 小范围功能、交互、文案或缺陷修正：增加第三个数字，例如 `v2.0.2`。
- 标签说明应包含范围、关键变化和验证结果。

## 完成与验证

- PC 原型：通过本地 HTTP 服务打开并检查主要流程；不要仅用 `file://` 双击验证。
- 移动端：执行依赖安装和构建，至少检查作业申请、作业监控查看及移动视口。
- PRD：通过本地 HTTP 服务打开 HTML，检查导航、图片、表格和内部链接。
- 所有界面任务至少考虑正常、加载、空、错误和无权限状态；不适用时在 PR 中说明。
- 完成任务后列出修改文件、已执行检查、未验证项和风险。
- 未经当次任务明确授权，不执行提交、推送、创建 PR、合并或发布。
