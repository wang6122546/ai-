/* 让安全交底配置列表同步展示作业类型与适用等级。 */
briefingConfigView=function(){return `<div class="risk-type-tabs">${riskConfigTypes.map((x,i)=>`<button class="${i===0?'active':''}">${x}</button>`).join('')}</div><div class="risk-measure-toolbar"><div>${riskLevels.map((x,i)=>`<button class="${i===0?'active':''}">${x}</button>`).join('')}</div></div><div class="config-scope-bar"><label>业务模式<select><option>普通作业</option><option>作业计划</option><option>一件一案</option></select></label></div><div class="briefing-head"><div><b>安全交底预设配置</b><span>维护作业步骤、安全防范措施、禁入区域、交底确认语和签名要求</span></div><button class="primary" id="addBriefing">＋新增</button></div><div class="table-wrap"><table><thead><tr><th>序号</th><th>作业类型</th><th>适用等级</th><th>标准化作业内容</th><th>签名要求</th><th>操作</th></tr></thead><tbody id="briefingRows">${briefingConfigs.map((x,i)=>`<tr data-index="${i}"><td>${i+1}</td><td>有限空间作业</td><td>一级</td><td>${x}</td><td>必须签名</td><td><button class="detail briefing-edit">编辑</button><button class="danger-text briefing-delete">删除</button></td></tr>`).join('')}</tbody></table></div>`}
