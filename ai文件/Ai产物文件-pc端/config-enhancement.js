(function(){
function openBound(mode,index){const b=mode==='briefing',a=b?briefingConfigs:finishStandards,v=index==null?'':(b?a[index]:a[index][1]),t=index==null?'动火作业':(b?'有限空间作业':a[index][0]);document.body.insertAdjacentHTML('beforeend',`<div class="config-drawer-layer"><button class="config-drawer-mask"></button><aside class="config-drawer"><header><b>▶ ${index==null?'新增':'编辑'}${b?'安全交底作业步骤':'结束确认标准'}</b><button class="config-drawer-close">×</button></header><div class="config-drawer-body"><label><span>* 作业类型</span><select id="boundType">${riskConfigTypes.map(x=>`<option ${x===t?'selected':''}>${x}</option>`).join('')}</select></label><label><span>* 适用等级</span><div class="bound-level-checks">${['特级','一级','二级','三级'].map(x=>`<label><input type="checkbox" name="boundLevel" value="${x}"> ${x}</label>`).join('')}</div></label><label><span>* ${b?'标准化作业内容':'结束确认标准'}</span><textarea id="boundText">${v}</textarea></label></div><footer><button class="ghost config-drawer-cancel">取消</button><button class="primary" id="saveBound">确定</button></footer></aside></div>`);const l=document.querySelector('.config-drawer-layer'),c=()=>l.remove();l.querySelector('.config-drawer-mask').onclick=l.querySelector('.config-drawer-close').onclick=l.querySelector('.config-drawer-cancel').onclick=c;l.querySelector('#saveBound').onclick=()=>{const x=l.querySelector('#boundText').value.trim(),ls=[...l.querySelectorAll('[name=boundLevel]:checked')];if(!x||!ls.length)return toast('请填写内容并选择适用等级');b?(index==null?a.push(x):a[index]=x):(index==null?a.push([l.querySelector('#boundType').value,x]):a[index]=[l.querySelector('#boundType').value,x]);c();document.querySelector('#configBody').innerHTML=configBody(b?3:4);setupConfigActions();toast('配置已保存')}}
document.addEventListener('click',e=>{const x=e.target.closest('#addBriefing,.briefing-edit,#addFinishStandard,.finish-edit');if(!x)return;e.preventDefault();e.stopImmediatePropagation();const b=x.matches('#addBriefing,.briefing-edit'),r=x.closest('tr');openBound(b?'briefing':'finish',r?+r.dataset.index:null)},true);
})();
