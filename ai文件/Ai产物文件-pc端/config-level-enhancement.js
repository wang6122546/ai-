/* 作业等级配置补充：上传方案、票据有效期、气体检测。 */
(function(){
  function openLevel(index){
    document.querySelector('.config-drawer-layer')?.remove();
    const x=index===null?['动火作业','','','否','0','0','0','是']:jobLevelConfigs[index];
    const yes=x[3]==='是', gas=x[7]||'否';
    document.body.insertAdjacentHTML('beforeend',`<div class="config-drawer-layer"><button class="config-drawer-mask"></button><aside class="config-drawer"><header><b>▶ ${index===null?'新增':'编辑'}作业等级</b><button class="config-drawer-close">×</button></header><div class="config-drawer-body"><label><span><i>*</i> 作业类型</span><select id="lvlType">${riskConfigTypes.map(v=>`<option ${v===x[0]?'selected':''}>${v}</option>`).join('')}</select></label><label><span>作业等级</span><input id="lvlName" value="${x[1]}" placeholder="请输入作业等级"></label><label><span>定义</span><textarea id="lvlDef" placeholder="请输入文本定义">${x[2]}</textarea></label><label><span><i>*</i> 必须上传作业方案</span><div class="radio-row"><label><input type="radio" name="lvlScheme" value="是" ${yes?'checked':''}> 是</label><label><input type="radio" name="lvlScheme" value="否" ${yes?'':'checked'}> 否</label></div></label><label><span><i>*</i> 作业票最长有效期</span><div class="inline-control"><input id="lvlValid" type="number" min="0" value="${x[4]||0}"><em>小时</em></div></label><label><span><i>*</i> 是否进行气体检测</span><div class="radio-row"><label><input type="radio" name="lvlGas" value="是" ${gas==='是'?'checked':''}> 是</label><label><input type="radio" name="lvlGas" value="否" ${gas==='否'?'checked':''}> 否</label></div></label></div><footer><button class="ghost config-drawer-cancel">取消</button><button class="primary" id="saveLevelConfig">确定</button></footer></aside></div>`);
    const layer=document.querySelector('.config-drawer-layer'),close=()=>layer.remove();
    layer.querySelector('.config-drawer-mask').onclick=close;layer.querySelector('.config-drawer-close').onclick=close;layer.querySelector('.config-drawer-cancel').onclick=close;
    layer.querySelector('#saveLevelConfig').onclick=()=>{const data=[layer.querySelector('#lvlType').value,layer.querySelector('#lvlName').value.trim()||'—',layer.querySelector('#lvlDef').value.trim()||'待补充',layer.querySelector('input[name="lvlScheme"]:checked').value,layer.querySelector('#lvlValid').value||'0','0','0',layer.querySelector('input[name="lvlGas"]:checked').value];if(index===null)jobLevelConfigs.push(data);else jobLevelConfigs[index]=data;close();document.querySelector('#configBody').innerHTML=configBody(1);setupConfigActions();toast('作业等级配置已保存')};
  }
  document.addEventListener('click',e=>{if(e.target.closest('#addJobLevel')){e.preventDefault();e.stopImmediatePropagation();openLevel(null)}else if(e.target.closest('.level-edit')){e.preventDefault();e.stopImmediatePropagation();openLevel(+e.target.closest('tr').dataset.index)}},true);
})();
